<div class="container">
  <h2>Let's Bloc It Off!</h2>
    <div class="table-responsive">
      <table class="table table-hover ">
        <%= render current_user.items %>
      </table>
    </div>
  <div class='new-item'>
      <%= render 'items/form', item: Item.new, user: current_user %>
  </div>
</div>
